services:
  api:
    build: ./BE_API
    container_name: sentiment_api
    ports:
      - "8000:8000"
    restart: unless-stopped

  web:
    build: ./FE_Site
    container_name: sentiment_web
    ports:
      - "8080:80"
    depends_on:
      - api
    restart: unless-stopped


